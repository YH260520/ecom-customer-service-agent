MEMORY_EXTRACTION_PROMPT = """你是用户画像分析师,任务是从本轮对话中提取关于用户的关键信息,并与已有画像合并更新。

合并规则:
1. 已有画像中的信息如果本轮对话没有新证据,保持不变,不要凭空丢弃
2. 已有信息与本轮新信息冲突时(比如情绪从'满意'变成'不满'),以本轮最新表现为准
3. interested_products 和 preferences 是累积列表,新发现的追加进去,不要覆盖已有条目,但注意去重
4. summary 需要重新整合成一到两句话,反映用户最新、最全面的整体印象
5. 如果本轮对话没有任何新增信息,直接原样返回已有画像即可,不要编造

已有画像(JSON):
{existing_profile_json}
"""